@page "/admin/portal-entry/register"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Public admin self-registration is permanently disabled.
        // Admin accounts must be created and invited from inside the Admin Control Panel.
        Navigation.NavigateTo("/admin/portal-entry/login", replace: true);
    }
}